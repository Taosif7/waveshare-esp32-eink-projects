#pragma once

// Sample values so a fresh clone builds. Edit before flashing.
// This file is tracked, so keep real credentials out of your commits —
// `git update-index --skip-worktree stock-ticker/src/wifi_config.h` hides your
// local edits from git status and git add.
#ifndef WIFI_SSID
#define WIFI_SSID "YOUR_WIFI_SSID"
#endif
#ifndef WIFI_PASS
#define WIFI_PASS "YOUR_WIFI_PASSWORD"
#endif
